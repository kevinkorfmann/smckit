# Planning moved

The canonical delivery plan is [`ROADMAP.md`](ROADMAP.md). Method-level
completeness is tracked in `docs/parity/feature-ledger.json`; numerical
acceptance rules are tracked in `docs/parity/oracle-specifications.json`.
