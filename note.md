# Publication note status

The Application Note is evidence-gated. Drafting may proceed alongside
implementation, but submission requires the 1.0 checklist in `ROADMAP.md`.
Claims must link to immutable records produced by `workflow/publication/`.
